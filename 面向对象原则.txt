单一职责原则：每个模块就应该具有单一的功能，而不是多而全。
开闭原则：不修改父类，而是在父类的基础上拓展子类
里氏替换原则：子类可以替换父类，这是开闭原则的基础
依赖倒置原则：针对接口编程，而不是针对实现编程。 高层模块不依赖于子模块，两者依赖于较为稳定的抽象接口，抽象接口连接彼此

迪米特原则：强调模块之间的松耦合，模块尽可能应该降低自身属性的访问权限。模块之前如果不必直接通信，那么就不应该发生直接作用，如果其中一个模块需要
调用另外一个模块的方法是，可以是用第三方模块来帮助